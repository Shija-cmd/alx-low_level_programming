#include "main.h"
#include <stdio.h>

/**
 * main - Points the name of the program
 * @argc: number of command line arguments
 * @argv: Array name
 * Return: 0
 */
int main(int argc, char *argv[])
{
	print("%s\n", argv[argc * 0]);
	return (0);
}
